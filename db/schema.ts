import {sqliteTable,text,integer,primaryKey,index} from 'drizzle-orm/sqlite-core';
export const libraries=sqliteTable('libraries',{userId:text('user_id').primaryKey(),createdAt:integer('created_at').notNull()});
export const cards=sqliteTable('cards',{id:text('id').notNull(),userId:text('user_id').notNull(),content:text('content').notNull(),createdAt:integer('created_at').notNull(),updatedAt:integer('updated_at').notNull(),correct:integer('correct').notNull().default(0),wrong:integer('wrong').notNull().default(0),streak:integer('streak').notNull().default(0),lastReview:integer('last_review'),nextReview:integer('next_review').notNull(),lastResult:text('last_result'),version:integer('version').notNull().default(1)},t=>[primaryKey({columns:[t.userId,t.id]})]);
export const reviews=sqliteTable('reviews',{id:text('id').notNull(),userId:text('user_id').notNull(),cardId:text('card_id').notNull(),correct:integer('correct').notNull(),reviewedAt:integer('reviewed_at').notNull(),mode:text('mode').notNull(),response:text('response').notNull()},t=>[primaryKey({columns:[t.userId,t.id]}),index('idx_reviews_user_time').on(t.userId,t.reviewedAt)]);
